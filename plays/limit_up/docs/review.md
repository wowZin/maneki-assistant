# 复盘机制 V2.4

## 概述

每日 15:00 收盘后自动触发复盘流程。V2.4 新增：
- **双排序对比**: 加权总分 vs Top3择优并排评估
- **攻击独特性因子**: 单独计算该因子的鉴别力
- **情绪熔断记录**: 熔断触发次数和冰点试探结果

## 复盘数据来源

| 数据 | 来源 | 路径 |
|------|------|------|
| 当日预测记录 | zt-team-leader | /srv/zt-team-leader/data/predictions/YYYY-MM-DD.json |
| 实际涨跌结果 | 行情API | 盘后数据接口 |
| 历史权重配置 | zt-team-leader | /srv/zt-team-leader/data/weights.json |
| 历史复盘存档 | zt-team-leader | /srv/zt-team-leader/data/reviews/YYYY-MM-DD.json |

## 复盘流程

### 1. 结果匹配

```
对当日每条推送股票(去重):
  - 查询该股票当日最终涨跌幅
  - 判定: 涨停(hit) / 未涨停(miss)
  - 判定: 胜(win) / 负(lose) — 收盘涨幅 > 扫描时涨幅 即为胜
  - 记录: weighted_score, top3_score, actual_result, hit/miss
```

### 2. 双跑对比（V2.4）

**2a. 双排序对比**（加权 vs Top3择优）：

分别计算加权总分和Top3择优的命中率：

```
加权总分排序:
  Top5:  X/5  命中率 Y%
  Top10: X/10 命中率 Y%
  
Top3择优排序:
  Top5:  X/5  命中率 Y%
  Top10: X/10 命中率 Y%
  
结论: 当前市场下 [加权/Top3] 更有效
```

**2b. 权重AB对比**（旧权重 vs 新权重，权重变更时启用）：

从同一份维度评分数据，用新旧两套权重分别算总分，对比命中率。
不重复跑pipeline（纯数学运算，零API开销）：

```
旧权重(f=0.7/t=0.4/ff=1.5/s=1.0/st=1.4):
  推送N只 → 命中M只 → 命中率 M/N%

新权重(f=1.5/t=1.0/ff=0.5/s=1.2/st=1.5):  
  推送N'只 → 命中M'只 → 命中率 M'/N'%

结论: [旧/新]权重更优, 建议[回滚/维持/继续观察]
```

> 权重变更后至少跑3个交易日AB，确认新权重持续优于旧权重再固化。

### 3. 鉴别力计算

对每个子 agent:

```
鉴别力 = avg_score(命中股票) - avg_score(未命中股票)
命中率 = 命中数 / (命中数 + 未命中数) × 100%
```

鉴别力含义:
- > 0.6: 该agent评分能有效区分涨停/非涨停
- 0.3 ~ 0.6: 有一定区分能力
- < 0.3: 区分能力弱
- < 0: 评分方向反了，需要检查

#### 新增：攻击独特性因子鉴别力（V2.4）

```
攻击独特性鉴别力 = avg(攻击分_命中) - avg(攻击分_未命中)

若该因子鉴别力持续低于其他4因子 → 考虑降权或替换
```

### 4. 熔断记录（V2.4新增）

```
当日情绪状态:
  炸板率: XX%
  跌停家数: XX
  否决6熔断触发: 是/否 (XX:XX触发, XX:XX解除)
  否决7冰点试探: 是 (标的: XXX 结果: 涨停/炸板)
```

### 5. 权重调整

```
if 鉴别力 > 0.6:    delta = +5
if 0.3 < 鉴别力 <= 0.6: delta = 0
if 0 < 鉴别力 <= 0.3:   delta = -5
if 鉴别力 <= 0:         delta = -10

新权重 = clamp(当前权重 + delta, 10, 40)
归一化: 每个权重 = 新权重 / 总和 × 100
```

### 6. 案例归档

每日保存:
- 加权排序命中最高/误判最重的 Top5
- Top3排序命中最高/误判最重的 Top5
- 攻击独特性因子表现最佳的3只股票
- 供后续模型调优参考

### 7. 报告生成与推送

- 构建飞书复盘卡片（核心指标见下方模板）
- 发送到复盘专用 webhook
- 本地存档到 reviews/ 目录

## 复盘报告模板（V2.4更新）

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  Maneki 复盘报告 YYYY-MM-DD
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

【核心指标】
  推送总数: XX
  实际涨停: XX
  命中率:   XX%
  胜率:     XX%

【双跑对比】
  2a. 排序对比:
     加权 Top5:  X/5  Top10:  X/10
     Top3 Top5:  X/5  Top10:  X/10
     效率: [加权/Top3] 领先
  
  2b. 权重AB (权重变更期间):
     旧权重推送N只 命中M只 命中率M/N%
     新权重推送N'只 命中M'只 命中率M'/N'
     结论: [旧/新]权重更优

【维度鉴别力】
  基本面:    XX (有效/弱/无效)
  技术面:    XX
  资金面:    XX
  情绪面:    XX
  短线博弈:  XX
  攻击独特性: XX (V2.4新增)

【熔断状态】
  炸板率:     XX%
  跌停家数:   XX
  熔断触发:   是/否
  冰点试探:   是 (XXX) / 否

【调整建议】
  1. 攻击独特性因子鉴别力=XX, 建议[保留/降权]
  2. Top3效果[优于/劣于]加权, 建议[并行/主用一种]
```

## 周期统计

| 统计维度 | 频率 | 内容 |
|---------|:----:|------|
| 日度 | 每日15:30 | 命中率、双排序对比、鉴别力、熔断记录 |
| 周度 | 每周日 | 本周平均命中率、权重变化趋势、Top3 vs 加权周胜率 |
| 月度 | 每月末 | 月度综合评估、攻击独特性因子IC、模型调优建议 |

## 总分概念（V2.4说明）

| 指标 | 是否保留 | 说明 |
|------|:--------:|------|
| 加权总分(0-100) | ✅ | 默认排序方式，继续使用 |
| Top3择优分 | ✅ 新增 | 并行排序，捕捉极端信号 |
| 维度子分(0-100) | ✅ | 各维度独立评分保留不变 |
| 综合置信度 | ✅ | 继续推送，标注"加权/Top3" |

总分依然存在，只是排序多了个选择。两套排名同时推送到飞书供参考。

## V2.4 权重配置变更记录（2026-05-22）

### 变更原因

基于321只涨停股30天回测数据和5/22盘中40只实时数据：
- 旧权重给资金面最高权(1.5→1.8)，但资金面70%股票得0分，高权被稀释
- 基本面区分度最强(P75=62)但权重仅0.7，高分贡献被低估
- 导致加权总分无法拉开，推送阈值50分几乎无人触及

### 变更内容

| 维度 | 旧权重 | 新权重 | 变更逻辑 |
|------|:--:|:--:|------|
| 基本面 | 0.7 | **1.5** | 91%有效分，区分度最强 → 给最高权 |
| 技术面 | 0.4 | **1.0** | 73%有效分，中位区分度 |
| 资金面 | 1.5 | **0.5** | 仅30%非零 → 降为加分项，不稀释总分 |
| 情绪面 | 1.2 | **1.2** | 95%有效分，维持 |
| 短线博弈 | 1.4 | **1.5** | 100%有效分，微调 |

- 加权分母: 5.0 → 5.7 (加权平均)
- 推送阈值: 50 → 35 (对应新权重下P90位置)
- 置信度: 高80→60, 中高65→45, 中等50→35, 低49→34

### 5/22盘中验证 (40股)

| | 旧权重 | 新权重 | Δ |
|---|---|---|---|
| P50 | 13.3 | 19.8 | +6.5 |
| P75 | 17.6 | 25.3 | +7.7 |
| P90 | 23.2 | 28.3 | +5.0 |
| max | 31.3 | 33.9 | +2.6 |

### 待验证

- 完整交易日(含封板/龙虎榜/竞价数据)能否触达35阈值
- 下一复盘周期对比旧权重命中率
